## .NET / Visual Studio
bin/
obj/
*.user
*.suo
.vs/
*.userprefs
TestResults/

## Build results
[Dd]ebug/
[Rr]elease/
x64/
x86/

## NuGet
*.nupkg
packages/

## Logs
*.log
CoderCommander_crash.log

## debug.ps1 process-tracking state (PID/HWND cache under %TEMP%, but covered here in case it
## ever gets written locally)
*.pid

## Local documentation and configuration (developer-only, not in repo)
AGENTS.md
CLAUDE.md
.claude/
debug.ps1
DEBUG.md
FIXES.md
AUDIT-FULL-PROMPT.md

## UI-automation test suite (FlaUI + NUnit) - developer-only, same as the audit docs above.
## Nothing in the main project references it (the ProjectReference runs UiTests -> CoderCommander,
## not the reverse) and there is no .sln, so a clone without this folder still builds normally.
UiTests/


